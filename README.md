# *GOS implementation for RISC OS

This repository will contain a module which provides the *GOS implementation for RISC OS in C.

The previous implementation was built into the Kernel of RISC OS Classic and written in assembly language.

